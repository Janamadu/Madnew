<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".himshi2"></activity>
        <activity android:name=".himshi1" />
        <activity android:name=".Him2Activity" />
        <activity android:name=".MainALLTableActivity" />
        <activity android:name=".MainMarks_inputActivity" />
        <activity android:name=".MainMarksVWActivity" />
        <activity android:name=".MainSignupActivity" />
        <activity android:name=".MainLoginActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>